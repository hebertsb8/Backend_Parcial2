# .gitignore para proyecto Django - PROTEGE CREDENCIALES Y DATOS LOCALES

# Python
*.py[cod]
*$py.class
*.so
.Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Django
*.log
local_settings.py
# Base de datos SQLite local y backups (no versionar datos locales)
db.sqlite3
db.sqlite3-*
db.sqlite3.*
*.sqlite3

# Archivos estáticos / media generados
media/
staticfiles/
static/

# Virtual Environments
venv/
env/
.venv/
ENV/
env.bak/
venv.bak/
pyvenv.cfg
Pipfile.lock

# IDEs / editors
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.swp
*.swo
*~
*.tmp

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.stackdump

# Environment variables and secrets
.env
.env.*
.env.local
.env.production
.env.staging
credentials.json
secrets.json
*.secret
config/secrets.json

# Firebase / Google Cloud service account keys
smart365-88674-firebase-adminsdk-fbsvc-4b783ba6cf.json
firebase-credentials.json
*firebase-credentials*.json
firebase*.json
google-cloud-credentials.json
*-credentials.json
google*.json
GOOGLE_APPLICATION_CREDENTIALS
gcp-credentials.json

# Tests / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/
nosetests.xml
.tox/
.cache

# Packaging / build
build/
dist/
*.egg-info/
pip-wheel-metadata/
.eggs/

# Logs and runtime files
logs/
*.log
*.pid
celerybeat-schedule
celerybeat.pid

# Node (si hay frontend integrado)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# ML Models (versionar solo metadatos, no modelos pesados)
ml_models/*.pkl
ml_models/*.joblib
ml_models/*.h5
ml_models/*.pb
ml_models/sales_scaler.pkl
ml_models/recommender_model.pkl
!ml_models/models_metadata.json
!ml_models/training_config.json

# Misc backups
*.bak
*.backup
*.old
*.orig

# Documentación interna que no se versiona
frontend_guides/
MANUAL_API_TESTS.txt
API_FRONTEND_REFERENCE.txt
casos_de_uso_implementados.txt
endpoints_mapeo_casos_de_uso.txt

# Mantener estos artefactos de metadatos en repo (excepciones)
!ml_models/models_metadata.json
!Parcial2_Backend_API.postman_collection.json

# Fin de .gitignore

